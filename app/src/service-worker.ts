/// <reference types="@sveltejs/kit" />
/// <reference lib="webworker" />

// This service worker is now handled by the SvelteKitPWA plugin
// with generateSW strategy. The push notification handlers are in
// /static/sw-push-handlers.js and imported via workbox configuration.

// Keep this file minimal as it will be replaced by the generated service worker
console.log('[SW] SvelteKit service worker - will be replaced by generated PWA service worker');
